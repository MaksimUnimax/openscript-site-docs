# AGENTS.md - OpenScript Site Docs pointer

This nested file points to the repo root contract at `/opt/openscript-site-docs/AGENTS.md`.

Use the root contract plus the task prompt as the authoritative rules for this repository.

This repo is for OpenScript / AI Starter Community site docs, not runtime or unrelated projects.

Reminder:
- one run = one task
- exact docs only
- no guessing
- no secrets
- no runtime changes
- explicit docs repair/import runs may create or import missing docs instead of stopping
