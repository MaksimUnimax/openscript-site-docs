<!-- source: https://core.telegram.org/bots/api -->
# Update

- Incoming update object.
- At most one optional field is normally present.
- `update_id` increases sequentially and can be used to ignore duplicates or restore order.
- Update types documented include `message`, `edited_message`, `channel_post`, `edited_channel_post`, `callback_query`, and more.
