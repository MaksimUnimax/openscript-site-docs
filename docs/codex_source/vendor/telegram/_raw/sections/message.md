<!-- source: https://core.telegram.org/bots/api -->
# Message

- Message object represents a message.
- `message_id` is unique inside the chat.
- `from` is optional and may be empty for channel messages.
- `chat` identifies the delivery context.
- `message_thread_id` exists for forum topics and private chats with forum topic mode.
